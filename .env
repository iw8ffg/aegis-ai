# File .env - Configurazione di Aegis AI

# Credenziali Database (non usate in questo esempio base, ma pronte per l'espansione)
POSTGRES_DB=aegis_db
POSTGRES_USER=aegis_user
POSTGRES_PASSWORD=una_password_molto_sicura

# Configurazione Server Email (per la funzione "Inoltra via Mail")
# Compila con i tuoi dati reali per far funzionare l'invio
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tua_email@gmail.com
SMTP_PASSWORD=tua_password_app_gmail # Usa una password per app di Google

# Indirizzo email di destinazione di default per i report
DEFAULT_RECIPIENT_EMAIL=destinatario@esempio.com
